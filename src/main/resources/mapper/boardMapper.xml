<?xml version="1.0" encoding="UTF-8" ?>    
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.green.board.mapper">
	<select id="getBoardList"
			resultType="com.green.board.dto.BoardDTO"> 
<!-- import문이 없으면 ""안에 package이름 다써야함 -->>
	SELECT
    	IDX
	    ,MENU_ID
	    ,TITLE
	    ,CONTENT
	    ,WRITER
	    ,TO_CHAR(REGDATE, 'YYYY-DD-MM') REGDATE
	    ,HIT
	FROM
	    BOARD
	WHERE
	    MENU_ID = #{menu_id}
	ORDER BY
	    IDX DESC;
	
	</select>
</mapper>
