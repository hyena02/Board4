<?xml version="1.0" encoding="UTF-8" ?>    
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

 
<mapper namespace="com.green.user.mapper.UserMapper">

<!-- 유저 추가 -->
	<insert id="insertUser">
		INSERT INTO TUSER (USERID, PASSWD,USERNAME,EMAIL,UPOINT,REGDATE)
		VALUES ( #{userid}, #{passwd},#{username},#{email},1000,sysdate)
	</insert>
<!-- 유저 목록 -->
	<select id="getUserList">
		SELECT USERID
			,PASSWD
			,USERNAME
			,EMAIL
			,UPOINT
			,TO_CHAR(REGDATE, 'YYYY-MM-DD')REGDATE
		FROM
			TUSER
		ORDER BY USERID ASC	
	</select>
	<select id="getUser">
		SELECT USERID
			,PASSWD
			,USERNAME
			,EMAIL
			,UPOINT
			,REGDATE
		FROM
			TUSER
		WHERE
			USERID = #{userid}	
	</select>
	
<!-- 아이디 중복 확인 -->
	<select id="getIdDupCheck">
		SELECT  USERID
			,PASSWD
			,USERNAME
			,EMAIL
			,UPOINT
			,TO_CHAR(REGDATE, 'YYYY-MM-DD')REGDATE
		FROM	TUSER
		WHERE   USERID = #{userid}	
	</select>


<!-- 유저 삭제 -->
	<delete id="deleteUser">
		DELETE FROM TUSER
		WHERE userid = #{userid}
	</delete>	
	
	
<!-- 유저 수정 : oldpw, arg0, param1 -->
<update id="updateUser">
	UPDATE  TUSER
	SET		 PASSWD   = #{userDto.passwd}
			,USERNAME = #{userDto.username}
			,EMAIL	  = #{userDto.email}
	WHERE	USERID 	  = #{userDto.userid}
	AND		PASSWD    = #{oldpw}
</update>

<!-- 사용자 수정 : map,
		passwd(작동)
		map.passwd(오류발생)-->
<update id="updateUser2">
	UPDATE  TUSER
	SET		 PASSWD   = #{passwd}
			,USERNAME = #{username}
			,EMAIL	  = #{email}
	WHERE	USERID 	  = #{userid}
	AND		PASSWD    = #{oldpw}
</update>
</mapper>
